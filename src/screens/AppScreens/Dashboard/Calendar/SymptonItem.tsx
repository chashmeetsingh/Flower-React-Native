import React, {Component} from 'react';import {View, Text, Image, StyleSheet} from 'react-native';interface SymptomItemProps {  image: string;  isSelected: boolean;  title: string;}export default class SymptonItem extends Component<SymptomItemProps, {}> {  render() {    return (      <View style={{display: 'flex', alignItems: 'center'}}>        <Image          source={this.props.image}          style={{height: 60, width: 60, resizeMode: 'contain'}}        />        {this.props.isSelected ? (          <View style={styles.selected}>            <Text style={[styles.buttonWithImageHeading, {color: '#F58015'}]}>              {this.props.title}            </Text>            <View style={styles.selectedIcon} />          </View>        ) : (          <Text style={styles.buttonWithImageHeading}>{this.props.title}</Text>        )}      </View>    );  }}const styles = StyleSheet.create({  buttonWithImageHeading: {    color: '#5F5D70',    fontSize: 17,    lineHeight: 23,    textAlign: 'center',    marginTop: 4,  },  selected: {    display: 'flex',    alignItems: 'center',  },  selectedIcon: {    backgroundColor: '#F58015',    height: 8,    width: 8,    marginTop: 8,    borderRadius: 4,  },});